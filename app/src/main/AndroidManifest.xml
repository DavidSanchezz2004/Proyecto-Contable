<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:label="TransferenciasOCR"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TransferenciasOCR">
        <activity android:name=".view.list.ListActivity" />
        <activity android:name=".view.editor.EditorActivity" />
        <activity android:name=".view.importer.ImportActivity" />
        <activity
            android:name="pe.ds.transferapp.view.export.ExportActivity"
            android:exported="false" />
        <activity
            android:name="pe.ds.transferapp.security.PinActivity"
            android:exported="false"/>




        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>